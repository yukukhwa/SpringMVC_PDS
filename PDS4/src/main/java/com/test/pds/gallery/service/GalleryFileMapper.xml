<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.test.pds.gallery.service.GalleryFileMapper">
	<!-- gallery insert가 성공적으로 실행이 되 id값이 gallery객체내 galleryId에 저장되면 나머지 galleryFile insert시킨다 -->
	<insert id="insertGalleryFile" 
			parameterType="com.test.pds.gallery.service.Gallery">
		INSERT INTO gallery_file
			(gallery_id
			, gallery_file_name
			, gallery_file_ext
			, gallery_file_type
			, gallery_file_size)
		VALUES 
		(#{galleryId}
		, #{galleryFile.galleryFileName}
		, #{galleryFile.galleryFileExt}
		, #{galleryFile.galleryFileType}
		, #{galleryFile.galleryFileSize})
	</insert>
</mapper>